/*
 * PointScreen Messages
 *
 * This contains all the text for the PointScreen Screen.
 */

export const scope = 'app.screen.PointScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the PointScreen Screen!',
    },
  },
};

export default messages;
