/*
 * TransactionScreen Messages
 *
 * This contains all the text for the TransactionScreen Screen.
 */

export const scope = 'app.screen.TransactionScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the TransactionScreen Screen!',
    },
  },
};

export default messages;
