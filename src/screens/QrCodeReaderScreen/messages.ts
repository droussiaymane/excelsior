/*
 * QrCodeScreen Messages
 *
 * This contains all the text for the QrCodeScreen Screen.
 */

export const scope = 'app.screen.QrCodeScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the QrCodeScreen Screen!',
    },
  },
};

export default messages;
