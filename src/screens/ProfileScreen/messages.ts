/*
 * ProfileScreen Messages
 *
 * This contains all the text for the ProfileScreen Screen.
 */

export const scope = 'app.screen.ProfileScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ProfileScreen Screen!',
    },
  },
};

export default messages;
