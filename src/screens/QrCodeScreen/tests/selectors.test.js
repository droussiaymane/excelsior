// import { selectQrCodeScreenDomain } from '../selectors';

describe('selectQrCodeScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
