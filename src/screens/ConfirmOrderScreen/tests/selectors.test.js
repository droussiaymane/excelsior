// import { selectConfirmOrderScreenDomain } from '../selectors';

describe('selectConfirmOrderScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
